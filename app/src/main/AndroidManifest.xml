<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.decisionsupport">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_decision_support_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".home.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".selectassistmethod.SelectAssistMethodActivity" />

        <activity android:name=".ahpmethod.AhpDashboard.AhpDashboardActivity" />

        <activity android:name=".ahpmethod.AhpSteps.AhpStepperActivity"/>

    </application>

</manifest>